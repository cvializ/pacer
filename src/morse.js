const morse = (signal) => {
    const cell$ = fromArray(signal).pipe(

    );
};

const n$ = morse('XX X  XX X  XX X  ');
const a$ = morse('  X XX  X XX  X XX');


gainNode$ = ({ gain: noop });
foo$.subscribe(({ time, value }) => {
    gainNode.gain.setValueAtTime(0, t);
});
